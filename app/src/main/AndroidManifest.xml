<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!--
      REQUIRED, and not for the reason it looks like.

      As a permission this is inert: it is signature-level, never granted to a
      normal app, and injection is actually gated by the OP_MOCK_LOCATION
      app-op. But Settings' AppPicker builds the "Select mock location app"
      list by scanning each package's requestedPermissions for this exact
      name and skipping anything that does not declare it. Remove the line and
      the app silently disappears from that list — with no error anywhere.
    -->
    <!--
      Both lint checks are suppressed on purpose: ProtectedPermissions because
      the declaration is a marker rather than a grant, and MockLocation because
      its advice ("move this to a debug-only manifest") assumes an app that
      mocks location for its own tests. Here mocking location *is* the shipped
      product, so it belongs in the main manifest.
    -->
    <uses-permission
        android:name="android.permission.ACCESS_MOCK_LOCATION"
        tools:ignore="ProtectedPermissions,MockLocation" />

    <application
        android:name=".MockLocationApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MockLocation"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:theme="@style/Theme.MockLocation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.MockLocationService"
            android:foregroundServiceType="location"
            android:exported="false"
            android:stopWithTask="false" />
    </application>
</manifest>
